export class Vertice {
    data: {
        id: string;
    }

    constructor(id: string) {
        this.data = {
            id: id
        };
    }

    // setId(id: string) {
    //     this.id = id;
    // }
}